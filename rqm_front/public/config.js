window.APP_CONFIG = {
    // backendUrl: full backend API URL (can be proxied or left empty for same origin)
    backendUrl: "",
    // basePath: path prefix where the app is hosted (e.g. '/app'). Empty = root or relative.
    basePath: "",
    // assetsPath: where static assets are served; relative by default
    assetsPath: ""
};
